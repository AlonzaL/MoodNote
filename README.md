# 📝 MoodNote - Android App

**MoodNote** — это минималистичное Android-приложение для ведения дневника настроения, построенное на современном технологическом стеке. Оно позволяет пользователям быстро записывать свои эмоции и мысли, сохраняя их в простом и наглядном списке.

Проект является отличным примером применения чистой архитектуры (MVVM), Jetpack Compose и Hilt для внедрения зависимостей.

| Главный экран | Экран деталей |
|:---:|:---:|
| <img src="https://github.com/user-attachments/files/24415969/default.bmp" width="300"/> | <img src="https://github.com/user-attachments/files/24415971/2.bmp" width="300"/> |

## ✨ Ключевые возможности

*   **Добавление заметок:** Пользователь может создать новую запись, указав описание своего настроения и выбрав соответствующий эмодзи (😊, 😢, 😡, 😴).
*   **Список заметок:** Все созданные заметки отображаются в виде списка, отсортированного по времени добавления. Каждая запись содержит эмодзи, описание и дату.
*   **Удаление заметок:** Простое удаление заметки по клику на нее в списке.
*   **Реактивный UI:** Интерфейс автоматически обновляется при добавлении или удалении заметок благодаря использованию `StateFlow` и Jetpack Compose.
*   **Валидация ввода:** Кнопка "Сохранить" становится активной только после ввода текста, что улучшает пользовательский опыт.

## 🛠️ Архитектура и технологии

Проект построен на принципах чистой архитектуры и следует паттерну **MVVM (Model-View-ViewModel)**.

*   **UI Layer (`View`):**
    *   Создан полностью на **Jetpack Compose**.
    *   Экраны `MoodListScreen` и `AddMoodScreen` являются `@Composable` функциями, которые получают данные из `ViewModel` и реагируют на изменения состояния.

*   **Presentation Layer (`ViewModel`):**
    *   `MoodViewModel` управляет состоянием UI и бизнес-логикой.
    *   Использует **StateFlow** для предоставления потока данных UI-слою, обеспечивая реактивность.
    *   Взаимодействует с `Repository` для выполнения операций с данными (добавление, удаление).

*   **Data Layer (`Model` & `Repository`):**
    *   `MoodNote`: data-класс, описывающий модель данных.
    *   `MoodRepository`: выступает как единственный источник данных. **В текущей реализации данные хранятся в памяти (`in-memory`)** и сбрасываются при перезапуске приложения.

*   **Внедрение зависимостей (Dependency Injection):**
    *   Используется **Hilt** для автоматического предоставления зависимостей.
    *   `MoodRepository` внедряется в `MoodViewModel` через конструктор, что делает код модульным и легко тестируемым.

### Технологический стек:

*   **Язык:** [Kotlin](https://kotlinlang.org/)
*   **UI:** [Jetpack Compose](https://developer.android.com/jetpack/compose) & [Material 3](https://m3.material.io/)
*   **Архитектура:** MVVM, Repository Pattern
*   **Внедрение зависимостей:** [Hilt](https://developer.android.com/training/dependency-injection/hilt-android)
*   **Асинхронность:** [Kotlin Coroutines](https://kotlinlang.org/docs/coroutines-overview.html) & [Flow](https://kotlinlang.org/docs/flow.html)
*   **Навигация:** [Navigation for Compose](https://developer.android.com/jetpack/compose/navigation)

## 📂 Структура проекта

*   **`data`**: Содержит `MoodNote.kt` (data-класс, описывающий модель заметки) и `MoodRepository.kt` (репозиторий, отвечающий за операции с данными).
*   **`di`**: Содержит `AppModule.kt` (Hilt-модуль, который "учит" Hilt, как создавать и предоставлять `MoodRepository`).
*   **`navigation`**: Содержит `NavGraph.kt` (описание всех маршрутов и переходов между экранами).
*   **`screens`**: Основная папка с UI. Содержит Composable-функции для каждого экрана: `MoodListScreen` и `AddMoodScreen`.
*   **`viewModel`**: Содержит `MoodViewModel`, который управляет состоянием UI и бизнес-логикой приложения.
*   **`MainActivity.kt`**: Главная Activity и точка входа в приложение.
*   **`MoodNoteApp.kt`**: Класс `Application`, необходимый для инициализации Hilt во всем приложении.


## 📈 Возможные улучшения (Roadmap)

*   **Постоянное хранилище:** Заменить `in-memory` репозиторий на базу данных **Room**, чтобы заметки сохранялись между сессиями.
*   **Редактирование заметок:** Добавить возможность изменять существующие заметки.
*   **Улучшение UX:**
    *   Добавить диалоговое окно для подтверждения удаления.
    *   Реализовать удаление по свайпу (жест "swipe-to-delete").
*   **UI/UX:** Внедрить анимации для более плавных переходов и взаимодействий.
*   **Тестирование:** Написать Unit-тесты для `ViewModel` и `Repository` для проверки бизнес-логики.
